# Project Guidelines

## Package Manager

Use `bun` for all commands — not `npm`, `npx`, or `yarn`.

- Dev server: `bun run dev`
- Build: `bun run build`
- Tests: `bun run test`
- Lint: `bun run lint`
